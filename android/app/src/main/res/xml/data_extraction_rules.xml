<?xml version="1.0" encoding="utf-8"?>
<!--
  SMS Guard stores the block/silence lists, drafts and scheduled message bodies
  in SharedPreferences. None of it should leave the device — exclude every
  domain from both cloud backup and device-to-device transfer (Android 12+).
  Pre-12 cloud backup is already disabled via android:allowBackup="false".
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
