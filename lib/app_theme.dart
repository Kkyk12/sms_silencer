import 'package:flutter/material.dart';

/// Palette for the minimalist messaging look (blue accents on white).
class AppColors {
  static const Color primary = Color(0xFF2F6BED); // accent / sent bubble / FAB
  static const Color sentBubble = Color(0xFF2F6BED);
  static const Color sentText = Colors.white;
  static const Color recvBubble = Color(0xFFEFF1F5);
  static const Color recvText = Color(0xFF1F2430);
  static const Color title = Color(0xFF101727);
  static const Color meta = Color(0xFF98A2B3); // grey timestamps / hints
  static const Color field = Color(0xFFF1F3F7); // input / pill background
}
